[Unit]
Description=PPTP link to %I
After=network.target
[Service]
Type=forking
# PIDFile=/run/ppp-%i.pid
ExecStart=/sbin/pon %I
ExecStop=/sbin/poff %I
ExecReload=/sbin/poff -r %I
Restart=on-failure
[Install]
WantedBy=multi-user.target
